import { css } from "@emotion/react";

export const linkFix = css`
  font-family: inherit;
  text-decoration: none;
  line-height: normal;
  color: inherit;
`;

export const buttonFix = css`
  cursor: pointer;

  border: 0;
  padding: 0;
  font-family: inherit;

  color: inherit;
  background-color: transparent;
`;

export const inputFix = css`
  box-sizing: border-box;

  color: inherit;
  font-family: inherit;
`;

export const textareaFix = css`
  box-sizing: border-box;

  color: inherit;
  font-family: inherit;
`;
